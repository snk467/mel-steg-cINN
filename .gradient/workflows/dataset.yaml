job-1:
  inputs:
    my-dataset:
      type: dataset
      with:
        ref: ds59h0ssl60rrf2
    uses: container@v1
    with:
    image: bash:5
    args: ["bash", "-c", "ls /inputs/my-dataset"]