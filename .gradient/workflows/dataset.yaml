  job-1:
    inputs:
      my-dataset:
        type: dataset
          with:
            ref: ds59h0ssl60rrf2
    outputs:
      spectograms:
        type: dataset
          with:
            ref: dst123abc
     uses: container@v1
     with:
      image: bash:5
      args: ["bash", "-c", "unzip /inputs/my-dataset -d /outputs/spectograms"]